import React from 'react';
//import styles from './Answer.css';

const answer = (props) => {
    return (
        <div>
            <p>{props.text}</p>
        </div>
    );
}

export default answer; 